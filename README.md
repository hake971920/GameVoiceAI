# GameVoiceAI

GameVoiceAI 是一个结合了屏幕捕获、视觉模型、语音识别和语音合成的多模态游戏助手项目。

## 项目结构

请参考 `docs/architecture.md` 了解架构详情。

## 快速开始

1. 安装依赖:
   ```bash
   pip install -r requirements.txt
   ```

2. 配置环境变量:
   复制 `.env.example` 为 `.env` 并填入配置。

3. 运行程序:
   ```bash
   python main.py
   ```
